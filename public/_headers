# Static assets are content-hashed per deploy; cache long at the edge.
/assets/*
  Cache-Control: public, max-age=31536000, immutable

# HTML: allow short edge caching to smooth TTFB for repeat crawls/visits.
# (Pages rebuilds invalidate the cache on every deployment.)
/*
  Cache-Control: public, max-age=300, must-revalidate
